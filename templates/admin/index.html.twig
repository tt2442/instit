{% extends 'admin.html.twig' %}

{% block title %}Partie administration
{% endblock %}

{% block body %}
	<style>
		.example-wrapper
		{
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}

		.example-wrapper code
		{
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Partie administration</h1>

		Ci-dessous les différentes parties du site :
		<ul>
			{# <li>Utilisateurs
			            <code><a href="{{ path('user_index') }}">Utilisateurs</a></code>
			        </li> #}
			{% set menus = ['article','categorie','materiel','liens_interne','liens_externe','promotion'] %}
			{% for menu in menus %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path(menu~'_index') }}">{{menu|capitalize}}s</a>
				</li>
			{% endfor %}


		</ul>
	</div>
{% endblock %}
