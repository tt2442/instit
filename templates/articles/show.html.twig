{% extends 'admin.html.twig' %}

{% block title %}Voir Articles 
{% endblock %}
{% block body %} <h1>Voir Articles </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{article.Users|json_encode}}</td>
</tr>
<td>{{article.Promotions|json_encode}}</td>
</tr>
<td>{{article.Categories|json_encode}}</td>
</tr>
<td>{{article.Fichiers|json_encode}}</td>
</tr>
<td>{{article.Materiels|json_encode}}</td>
</tr>
<td>{{article.Commentaires|json_encode}}</td>
</tr>
<td>{{article.commandes|json_encode}}</td>
</tr>
<tr>
<th>Id</th>
<td>{{article.id}}</td>
</tr>
<tr>
<th>Titre</th>
<td>{{article.Titre}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{article.Description}}</td>
</tr>
<tr>
<th>Multimedia</th>
<td>{{article.Multimedia}}</td>
</tr>
<tr>
<th>Etat</th>
<td>{{article.Etat}}</td>
</tr>
<tr>
<th>Quantite</th>
<td>{{article.Quantite}}</td>
</tr>
<tr>
<th>FraisEnvoi</th>
<td>{{article.FraisEnvoi}}</td>
</tr>
<tr>
<th>Prix</th>
<td>{{article.Prix}}</td>
</tr><td>
    <form method='post' action="{{ path('articles_delete', {'id':  article.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  article . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('articles_copy', {'id':  article.id }) }}">Dupliquer</a>
        <a href='{{ path('articles_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>{% endif %}
    </tbody>
    </table>
    </div><a href="{{ path('articles_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('articles/_delete_form.html.twig') }}
    {% endblock %}