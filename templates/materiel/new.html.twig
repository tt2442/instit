{% extends 'admin.html.twig' %}{% set action=path(app.request.attributes.get('_route'),
app.request.attributes.get('_route_params'))|split('/')|last
%}

{% block title %}{% if action=='new' %}Création {% else %}Duplication {% endif %} Materiel{% endblock %}

{% block body %}


<h1>{% if action=='new' %}Ajouter {% else %}Dupliquer {% endif %} Materiel</h1>

{{ form_start(form) }}

{{ form_widget(form) }}<button class="btn btn-primary" type="submit">Créer</button>

<a href="{{ path('materiel_index') }}"><button class="btn btn-secondary" type="button">Revenir à la liste</button></a>
{{ form_end(form) }}
{% endblock %}