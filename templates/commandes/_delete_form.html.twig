<form method="post" action="{{ path('commandes_delete', {'id': commande.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button class="btn btn-warning mt-2">Supprimer</button>
</form>