{% extends 'admin.html.twig' %}{% block title %}Edition Commentaires{% endblock %}

{% block body %}
<h1>Editer Commentaires </h1>

{{ form_start(form) }}
{{ form_row(form.user) }}{{ form_row(form.articles) }}{{ form_row(form.id) }}{{ form_row(form.Etat) }}{{ form_row(form.Message) }}{{ form_row(form.Date) }}
<button class="btn btn-primary mr-2" type="submit">Mettre à jour</button>
<a href="{{ path('commentaires_index') }}" class="btn btn-secondary mr-2" type="button">Revenir à la liste</button></a>

{{ form_end(form, {'render_rest': false}) }}
{{ include('commentaires/_delete_form.html.twig') }}
{% endblock %}