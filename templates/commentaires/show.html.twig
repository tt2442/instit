{% extends 'admin.html.twig' %}

{% block title %}Voir Commentaires 
{% endblock %}
{% block body %} <h1>Voir Commentaires </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{commentaire.user|json_encode}}</td>
</tr>
<tr>
<th>Etat</th>
<td>{% set options={'Approuvé':'approuve','Rejeté':'rejete','Caché':'cache',} %}
         {% set res=[] %}
                    {% for key,option in options %}
                    {% if option in commentaires.Etat %}
                    {% set res=res|merge([key]) %}
                    {% endif %}
                    {% endfor %}
                    {{res|json_encode}}</td>
</tr>
<tr>
<th>Message</th>
<td>{{commentaire.Message}}</td>
</tr>
<tr>
<th>Date</th>
<td>{{commentaire.Date ? commentaire.Date|date('Y-m-d H:i:s') : ''}}</td>
</tr><td>
    <form method='post' action="{{ path('commentaires_delete', {'id':  commentaire.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  commentaire . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('commentaires_copy', {'id':  commentaire.id }) }}">Dupliquer</a>
        <a href='{{ path('commentaires_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('commentaires_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('commentaires/_delete_form.html.twig') }}
    {% endblock %}